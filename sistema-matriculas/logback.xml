<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <!-- Define log directory -->
    <property name="LOG_DIR" value="./logs" />

    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender for ZooKeeper -->
    <appender name="ZK_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/zookeeper.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Keep 30 days of history -->
            <fileNamePattern>${LOG_DIR}/zookeeper-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- ZooKeeper-specific logging -->
    <logger name="org.apache.zookeeper" level="INFO" additivity="false">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ZK_FILE"/>
    </logger>

    <!-- Curator Framework logging (if used) -->
    <logger name="org.apache.curator" level="INFO" additivity="false">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="ZK_FILE"/>
    </logger>

</configuration>
